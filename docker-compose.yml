version: '3.8'
services:
  web:
    image: discover-cars
    ports:
      - "3000:3000"
  proxy:
    image: nginx:stable
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8080:80"
    depends_on:
      - web
